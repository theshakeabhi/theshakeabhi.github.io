// STUB(P1): replaced by package P1 (4 lagging colored dots behind the
// cursor — README §Custom Cursor + Trail).

export interface CursorTrailProps {
  count?: number;
}

export default function CursorTrail(_props: CursorTrailProps) {
  return null;
}
