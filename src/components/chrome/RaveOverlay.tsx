// STUB(P6): replaced by package P6 (50 confetti squares on the `fall`
// keyframe, dismissible "✶ RAVE MODE UNLOCKED ✶" banner — README §Easter
// Egg). The banner must mount outside any transformed ancestor (trap 1).

export interface RaveOverlayProps {
  on: boolean;
  onClose(): void;
}

export default function RaveOverlay(_props: RaveOverlayProps) {
  return null;
}
