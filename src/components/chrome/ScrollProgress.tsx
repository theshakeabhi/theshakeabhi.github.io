// STUB(P4): replaced by package P4 (fixed 14px candy-stripe progress bar
// + "{pct}% READ" pill — README §Scroll Progress Bar). Must stay OUTSIDE
// any transformed ancestor (trap 1).
import type { RefObject } from "react";

export interface ScrollProgressProps {
  targetRef?: RefObject<HTMLElement | null>;
}

export default function ScrollProgress(_props: ScrollProgressProps) {
  return null;
}
